trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo Hello, world v2!
  displayName: 'Run a one-line script v2'

- script: |
    echo Add a multi-line script v2
    echo One more line v2
  displayName: 'Run a multi-line script v2'